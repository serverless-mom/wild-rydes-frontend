{
  "name": "wild-rydes",
  "description": "Wild Rydes",
  "version": "0.1.0",
  "license": "MIT",
  "repository": "github:ServerlessOpsIO/wild-rydes",
  "dependencies": {},
  "devDependencies": {
    "serverless-python-requirements": "^4.0.3",
    "serverless-s3-sync": "^1.7.0"
  },
  "scripts": {
    "setup": "pyenv virtualenv 3.6 $(basename $(pwd)) && pyenv local $(basename $(pwd)) && pip install -r requirements.txt -r requirements-dev.txt"
  }
}
